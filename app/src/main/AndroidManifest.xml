<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.administrator.imagepager">
    <!--android:name="ook.yzx.tinker.Application"-->

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Activity.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".Activity.PagerActivity"
            android:theme="@style/PagerTheme" />
        <activity android:name=".Activity.DefeViewActivity" />
        <activity android:name=".Activity.MVPTestActivity" />
        <activity android:name=".Activity.MVVPActivity" />
        <activity android:name=".Activity.ThreadTestActivity" />
        <activity android:name=".Activity.ViewDragActivity" />
        <activity
            android:name=".Activity.BehaviorActivity"
            android:theme="@style/PagerTheme" />
    </application>

</manifest>